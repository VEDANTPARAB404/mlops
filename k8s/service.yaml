apiVersion: v1
kind: Service
metadata:
  name: ml-predictor
  namespace: ml-predictor
  labels:
    app: ml-predictor
spec:
  selector:
    app: ml-predictor
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: NodePort
